import { createIconSetFromIcoMoon } from 'react-native-vector-icons';
import { icoMoonConfig } from '../../assets/icons';

const MarvelIcons = createIconSetFromIcoMoon(icoMoonConfig);

const vectorIcons = {
	MarvelIcons,
};

export default vectorIcons;
